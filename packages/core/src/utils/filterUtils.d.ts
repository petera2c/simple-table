import { FilterCondition } from "../types/FilterTypes";
/**
 * Applies a filter condition to a cell value
 */
export declare const applyFilterToValue: (cellValue: any, filter: FilterCondition) => boolean;
