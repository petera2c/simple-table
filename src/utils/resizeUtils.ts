/**
 * Re-export from the resizeUtils module for backward compatibility.
 * Implementation is split across src/utils/resizeUtils/*.ts
 */
export {
  handleResizeStart,
  recalculateAllSectionWidths,
  handleResizeWithAutoExpand,
} from "./resizeUtils/index";
