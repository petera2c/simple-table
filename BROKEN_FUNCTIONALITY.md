# Broken Functionality (Failing Tests)

This list was generated by running each Storybook test individually. **24 tests failed**, 177 passed (201 total).

To reproduce: install dependencies, install Playwright browsers (`PLAYWRIGHT_BROWSERS_PATH="$(pwd)/.playwright-browsers" npx playwright install chromium`), build Storybook, serve it on port 6006, then run `npm run test-storybook -- --url http://127.0.0.1:6006 --verbose`.

---

## 01 - Basic Structure & Rendering

_No failing tests (ColumnWidthConfigurations fixed: fr/px normalization + container-width fill)._

---

## 08 - Column Width

| Story | Functionality |
|-------|---------------|
| **MinWidthConstraint** | `minWidth` respected when `autoExpandColumns: true` (expected width ≥ 195) |
| **AutoExpandColumnsBasic** | Auto-expand columns – equal-ish column width distribution (avg ±10%) |
| **MixedWidthStrategies** | Mixed width strategies – minWidth / maxWidth with auto-expand |
| **GridTemplateColumnsFormat** | Grid template columns format (e.g. `1fr` / template string) – assertion on computed style |
| **WideContainerBehavior** | Wide container – columns expand beyond 400px as expected |

---

## 12 - Cell Selection

| Story | Functionality |
|-------|---------------|
| **ColumnHeaderSelection** | Selecting a column via header cell |
| **MultipleColumnHeaderSelections** | Multiple column header selections |

---

## 13 - Column Resize

| Story | Functionality |
|-------|---------------|
| **BasicColumnResize** | Basic column resize (width change after drag) – assertion "expected 250 to be less than 5" suggests test/API mismatch |
| **ResizeMultipleColumns** | Resizing multiple columns – finding header cell / resize result |
| **ResizeToSmallerWidth** | Resizing column to smaller width |
| **ResizeWithMinWidth** | Resize respects `minWidth` (final width ≥ 195) |
| **ResizeAllColumns** | Resize all columns – width assertion |

---

## 14 - Live Updates

| Story | Functionality |
|-------|---------------|
| **UpdateDataAPIPrice** | `updateData` API – price cell update |
| **FlashAnimationDetection** | Flash animation on cell update |
| **UpdateDataAPIStock** | `updateData` API – stock cell update |
| **UpdateDataAPISales** | `updateData` API – sales cell update |
| **MultipleCellUpdates** | Multiple cell updates in one flow |

---

## 15 - Column Visibility

| Story | Functionality |
|-------|---------------|
| **HideMultipleColumns** | Hiding multiple columns |
| **ToggleColumnMultipleTimes** | Toggling column visibility multiple times |
| **ColumnCountChanges** | Column count updates after show/hide |

---

## 17 - Nested Tables

| Story | Functionality |
|-------|---------------|
| **BasicNestedTable** | Basic nested table rendering/behavior |

---

## 18 - Quick Filter

| Story | Functionality |
|-------|---------------|
| **BasicQuickFilterSimpleMode** | Quick filter (simple mode) – filter by department; assertion expected "Engineering", got "Sales"; name/data mismatch |
| **SmartModeMultiWord** | Quick filter smart mode (multi-word) – same department/result assertion |

---

## 19 - Accessibility

| Story | Functionality |
|-------|---------------|
| **TableStructureAriaAttributes** | Table structure and ARIA attributes (e.g. role, aria-*) |

---

## Summary by area

- **Column width / layout**: 4 (08)
- **Column resize**: 5 (13)
- **Live updates / updateData**: 5 (14)
- **Column visibility**: 3 (15)
- **Cell selection (header)**: 2 (12)
- **Quick filter**: 2 (18)
- **Nested tables**: 1 (17)
- **Accessibility**: 1 (19)

**Total: 24 failing tests across 8 test files.**
