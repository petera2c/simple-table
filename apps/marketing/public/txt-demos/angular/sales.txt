// sales-demo.component.ts
import { Component } from "@angular/core";
import { SimpleTableComponent } from "@simple-table/angular";
import type { Row } from "@simple-table/angular";
import { generateSalesData } from "./sales.demo-data";
import { SALES_HEADERS } from "./sales-headers";
import "@simple-table/angular/styles.css";

@Component({
  selector: "sales-demo",
  standalone: true,
  imports: [SimpleTableComponent],
  template: `
    <simple-table
      [columns]="headers"
      [rows]="rows"
      [enablePagination]="true"
      [rowsPerPage]="40"
      footerPosition="top"
      initialSortColumn="dealValue"
      initialSortDirection="desc"
      [autoExpandColumns]="true"
      [columnReordering]="true"
      [columnResizing]="true"
      [enableColumnEditor]="true"
      [selectableCells]="true"
      height="70dvh"
    ></simple-table>
  `,
})
export class SalesDemoComponent {
  readonly rows: Row[] = generateSalesData(240);
  readonly headers = SALES_HEADERS;
}
