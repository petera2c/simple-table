// infrastructure-demo.component.ts
import { Component } from "@angular/core";
import { SimpleTableComponent } from "@simple-table/angular";
import type { AngularHeaderObject, Row } from "@simple-table/angular";
import { infrastructureData } from "./infrastructure.demo-data"; // 50 sample servers
import { InfraCpuCellComponent } from "./infrastructure-cell-components";
import "@simple-table/angular/styles.css";

const headers: AngularHeaderObject[] = [
  { accessor: "serverId", label: "Server ID", type: "string", pinned: "left", width: "1.2fr", sortable: true },
  {
    accessor: "performance",
    label: "Performance Metrics",
    children: [
      // Built-in sparkline column type
      { accessor: "cpuHistory", label: "CPU History", type: "lineAreaChart", width: 150 },
      { accessor: "cpuUsage", label: "CPU %", type: "number", editable: true, width: 120, cellRenderer: InfraCpuCellComponent },
      // ...memory, disk, response columns
    ],
  },
  { accessor: "status", label: "Status", type: "enum", width: 130 },
];

@Component({
  selector: "infrastructure-demo",
  standalone: true,
  imports: [SimpleTableComponent],
  template: `
    <simple-table
      [defaultHeaders]="headers"
      [rows]="rows"
      [columnReordering]="true"
      [columnResizing]="true"
      [editColumns]="true"
      [selectableCells]="true"
      height="400px"
    ></simple-table>
  `,
})
export class InfrastructureDemoComponent {
  readonly rows: Row[] = infrastructureData;
  readonly headers: AngularHeaderObject[] = headers;

  // Stream live metrics by calling the table API's updateData(...) on an interval
}
