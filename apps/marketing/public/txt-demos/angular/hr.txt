// hr-demo.component.ts
import { Component } from "@angular/core";
import { SimpleTableComponent } from "@simple-table/angular";
import type { Row } from "@simple-table/angular";
import { hrConfig } from "./hr.demo-data";
import "@simple-table/angular/styles.css";

@Component({
  selector: "hr-demo",
  standalone: true,
  imports: [SimpleTableComponent],
  template: `
    <simple-table
      [columns]="headers"
      [rows]="rows"
      [enablePagination]="true"
      [rowsPerPage]="8"
      [customTheme]="{ rowHeight: 48 }"
      [columnReordering]="true"
      [columnResizing]="true"
      [selectableCells]="true"
      height="400px"
    ></simple-table>
  `,
})
export class HRDemoComponent {
  readonly rows: Row[] = [...hrConfig.rows];
  readonly headers = hrConfig.headers;
}
