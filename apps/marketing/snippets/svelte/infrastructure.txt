// InfrastructureDemo.svelte
<script lang="ts">
  import { SimpleTable } from "@simple-table/svelte";
  import type { SvelteColumnDef } from "@simple-table/svelte";
  import { infrastructureData } from "./infrastructure.demo-data"; // 50 sample servers
  import InfraCpuCell from "./InfraCpuCell.svelte";
  import "@simple-table/svelte/styles.css";

  let tableRef: any;

  const headers: SvelteColumnDef[] = [
    { accessor: "serverId", label: "Server ID", type: "string", pinned: "left", width: "1.2fr", sortable: true },
    {
      accessor: "performance",
      label: "Performance Metrics",
      children: [
        // Built-in sparkline column type
        { accessor: "cpuHistory", label: "CPU History", type: "lineAreaChart", width: 150 },
        { accessor: "cpuUsage", label: "CPU %", type: "number", editable: true, width: 120, cellRenderer: InfraCpuCell },
        // ...memory, disk, response columns
      ],
    },
    { accessor: "status", label: "Status", type: "enum", width: 130 },
  ];

  // Stream live metrics by calling tableRef.getAPI().updateData(...) on an interval
</script>

<SimpleTable
  bind:this={tableRef}
  columns={headers}
  rows={infrastructureData}
  columnReordering={true}
  columnResizing={true}
  enableColumnEditor={true}
  selectableCells={true}
  height="400px"
/>
