// billing-demo.component.ts
import { Component } from "@angular/core";
import { SimpleTableComponent } from "@simple-table/angular";
import type { Row } from "@simple-table/angular";
import { billingConfig } from "./billing.demo-data";
import "@simple-table/angular/styles.css";

@Component({
  selector: "billing-demo",
  standalone: true,
  imports: [SimpleTableComponent],
  template: `
    <simple-table
      [columns]="headers"
      [rows]="rows"
      [rowGrouping]="['invoices', 'charges']"
      initialSortColumn="amount"
      initialSortDirection="desc"
      [columnReordering]="true"
      [columnResizing]="true"
      [enableColumnEditor]="true"
      [selectableCells]="true"
      [oddColumnBackground]="true"
      height="400px"
    ></simple-table>
  `,
})
export class BillingDemoComponent {
  readonly rows: Row[] = billingConfig.rows;
  readonly headers = billingConfig.headers;
}
